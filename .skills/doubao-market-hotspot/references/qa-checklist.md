# 交付复核清单

## 第一阶段（对话框正文）

输出前逐项检查：

- [ ] 第一行固定风险提醒逐字正确
- [ ] 九个模块顺序与 `chat_contract.md` 一致；`##` 标题含定点 emoji 前缀（首屏无 `##`）
- [ ] 首屏含 headline、导语、作答+条件（**同一段**）、2×2 表；headline 为 `**📌 …**`（句首固定 📌，句末无句号）；**无** `分析对象：`/`标题：`/`信源范围：`/`核心观点摘要`/`问题`/`结论`/`✅ 改善`/`❌ 恶化` 等字段标签
- [ ] 首屏含 2×2 `关键数据` 表（无 `**关键数据：**` 标题行）；分隔行 `|:----:|:-----:|:----:|:-----:|`；每格数值有 `[n]` 且 URL 可打开；**表中数值不加粗**
- [ ] 首屏与 `## 📈` 之间有 `---`；首屏内部无 `---`、无引用块；**作答+条件段内部无空行**；**未**把首屏五块（风险提醒 / headline / 导语 / 作答+条件 / 表）合并成无空行连续长文
- [ ] **无**正文 `>` 引用块（模块 4 本段结论与其它模块相同，仅 `**本段结论：……**` 整段加粗）
- [ ] 首屏作答+条件**同一段**：作答 **≥1 处**关键分句加粗；条件 **1 句**常规字重（不加粗）；导语为事实句不加粗
- [ ] 每个主体模块有整段加粗的 `**本段结论：……**`（2–3 句）；含综合判断 + 与共识/预期差 + 对用户启示；本段结论与后续内容之间空一行
- [ ] **无** `**` 与文字间有空格（如 `** 本段结论： **`）；扫描全文 `** ` 与 ` **` 均为零
- [ ] 加粗仅四类：`headline`、`**本段结论：……**`（整段）、**关键分句**、**格式标签**；**无**长句整句加粗、**无**词/数字点状加粗
- [ ] 关键分句加粗，`[n]` 在 `**……**` 外；每句最多 1 处；全文关键分句（不含 headline、本段结论、格式标签）≤12 处；不为加粗而空行拆段（首屏作答+条件除外，须同段）
- [ ] **版式空行**（输出前必扫；见 `chat_contract.md`「段落间距」「输出前空行扫描」）：
  - [ ] 风险提醒 ↔ headline ↔ 导语 ↔ 作答+条件 ↔ 2×2 表：五块之间**各空一行**；作答+条件段内部**无**空行
  - [ ] 每个 `##` 模块：`**本段结论：……**` 与下一元素（事件卡 / 表 / 列表 / 展开段）之间**空一行**；不得本段结论紧接 `### 📰` 或表格
  - [ ] 同一模块内相邻长段落（≥2 句或单句 ≥60 字）之间**各空一行**
  - [ ] **未**把「禁止空行」泛化到作答+条件段以外（事件卡四字段、信号/风险条内部仍可不空行）
- [ ] 关键增量 **3–5** 条事件卡片（`### 📰 日期｜信息来源：…｜标题` + 四字段）；卡片间有 `---`
- [ ] 每条事件卡 `市场反应` 含定价层（预期差 / price in / 对冲因素），定价判断以**关键分句**加粗（可与事实句同段；非长句整句）
- [ ] 「如何影响市场」为四列 Markdown 表格，分隔行 `|:----:|:-----|:---------|:---------|`，**无**机制段替代；表内数字不加粗
- [ ] 接下来盯什么 3–4 条，含 📌/👀/✅/❌ 四行；条间有 `---`
- [ ] 每条信号 `👀 盯` 与 `❌ 恶化` 含量化阈值（纯文本，不加粗）
- [ ] 风险提示 **3–5** 条，含 ⚠️/🔴/🟢 三行；条间有 `---`；触发/证伪正文不加粗
- [ ] 各方观点四条含 📋📊🏦🔄 角色 emoji（仅角色标签加粗）
- [ ] 正文句末仅 `[n]` 编号；**无**句内 Markdown 链接、**无** `→`/`↓`
- [ ] 信息来源与正文 `[n]` 一一对应；**每条**第四段为 `http(s)://` 开头的真实文章链接
- [ ] **无** `（行情数据）` `（数据库）` `（实时行情）` `无链接` 等非 URL 第四段
- [ ] **无**「同花顺/Wind/彭博金融数据库」类无网页来源的独立编号；盘面数字已并入对应媒体稿 `[n]`
- [ ] URL 非占位、非站点首页泛链；至少 3 条可点击原文链接
- [ ] 无荐股、目标价、买卖点、仓位、涨跌承诺
- [ ] 固定结尾句逐字正确，且为**用户可见最后一行**（其后无空行、无附言、无 bullet 摘要）
- [ ] **无**固定结尾后的元叙述（「已按 Skill 完成」「核心结论如下」等）
- [ ] **无**改写或重复的飞书 CTA（仅允许固定结尾那一句）
- [ ] **无**暴露 Skill 名、阶段名、内部文件名
- [ ] 未输出 XML
- [ ] `chat_contract.md`「飞书准入门禁」五项全部通过（否则不得进入第二阶段）

## MD→XML 复核（飞书前）

- [ ] 已确认 `lark-doc` Skill 可用（见 `feishu-doc-style.md`「前置依赖」）
- [ ] 已扫描 `chat_contract.md`「飞书准入门禁」；任一项失败 → **硬阻断**
- [ ] 母版每个 XML 段落/表格单元格能在第一阶段正文找到原文（块级 Markdown 可去掉，行内 `<b>`/定点 emoji 须对应母版）
- [ ] `**本段结论：……**` 已映射为 `<p><b>本段结论：……</b></p>`（整段加粗，非仅标签）
- [ ] 关键分句 `**……**` 已映射为段落内 `<b>……</b>`，与母版一致；首屏作答+条件同段（仅关键分句 `<b>`）
- [ ] 未在 XML 中新增母版没有的加粗
- [ ] 未改写、未同义替换、未新增事实或来源
- [ ] XML 中无句内 `→`、无 Markdown 残留
- [ ] 模块1 风险提醒、模块9 风险提示、固定结尾问句**未**进入 XML
- [ ] 第一节「本段结论」取自首屏**作答+条件**段中作答部分前 1–2 句（非模块 4 本段结论或导语全文）
- [ ] 文档头含 `SECTION:document-metrics` 2×2 表，与首屏表一致
- [ ] 第四节表格行数与母版模块6 一致
- [ ] 第六节表格行数与母版模块8 信号条数一致
- [ ] 第七节来源 URL 均为 `<a href>`，与母版 `[n]` 一一对应
- [ ] 正文段落中 `[n]` 为纯文本，不包 `<a>`
- [ ] 尾部免责声明逐字来自 `disclaimers.json`

## 飞书 XML 复核

- [ ] 仅 lark-doc XML，无块级 Markdown 残留（无 `**`、`##`、`| … |`、`---`、`>`）
- [ ] 允许行内 `<b>` 与定点 emoji（✅❌📌📋📊🏦🔄 等）
- [ ] 七个 `h1` 章节标题字面量正确
- [ ] 顶部 title + 元数据 + callout（📌）+ headline + summary + **关键数据表**（对照 `example_feishu.xml` `SECTION:document-header` 与 `SECTION:document-metrics`）
- [ ] 第四节、第六节长文本列左对齐（`align="left"`）
- [ ] 第四节四列表格含 colgroup
- [ ] 第六节标题字面量为「六、哪些信号会验证核心观点？哪些信号代表观点证伪？」
- [ ] 第六节为四列表格（信号/观察口径/改善/恶化）
- [ ] 尾部「风险提示与免责声明」**仅**含 `disclaimers.json` 三段灰色正文，**无**模块9 风险 callout
- [ ] 已通过 `lark-doc` 写入；目检渲染正常

## 失败处理

- 第一阶段失败 → 修正文，不进入飞书流程
- `lark-doc` 未安装 → 说明并给安装指引，保留第一阶段正文
- MD→XML 失败 → 回第一阶段补正文（格式不符时），禁止在 XML 中现场改写成义句凑版
- XML/`lark-doc` 写入失败 → 说明阻塞，保留第一阶段正文；**不**降级为其它格式
